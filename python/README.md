# Machine Learning Example For Demonstration

First compile the machine learning model using the command:

"python example.py"

Next, output the data to the web app using the command:

"tensorboard --logdir=/tmp/tensorflow_ls --port=600"
